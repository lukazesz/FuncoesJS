// Utilizando o método .map para alterar os itens de um array, nesse caso imprimindo o seu dobro.
const numeros = [1, 2, 3, 4, 5];
const dobrados = numeros.map(num => num * 2);
console.log(dobrados); 
